# Git identity applied on a fresh machine when none is configured yet.
# Uncomment and fill in; bootstrap.sh never overwrites an existing identity.
#
# GIT_USER_NAME="Rob McGill"
# GIT_USER_EMAIL="robertjmcgill@proton.me"
